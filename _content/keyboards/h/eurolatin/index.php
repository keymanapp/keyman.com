<?php
  // This is a whole lot shorter than a web.config record,
  // and we need to support existing links so we can't
  // eliminate the redirect altogether.
  header('Location: /keyboards/sil_euro_latin');
  exit;
